# ------------------------ CMAKE SRC : Doc ------------------------ 

# create new project
project(Doc)

# create a variable that contains every cpp file to compile
set(SOURCES

)

# add every source file in the project
add_library(${PROJECT_NAME}${SOURCES})
